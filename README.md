# Image Uploader Starter

This workspace contains a minimal React frontend and a Flask backend that accepts image uploads.

Structure:
- `backend/` — Flask server (port 8000)
- `frontend/` — Vite + React app (port 5173)

See `backend/README.md` and `frontend/README.md` for run instructions.
