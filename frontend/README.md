# Frontend (React + Vite)

Install and run:

```bash
cd frontend
npm install
npm run dev
```

The frontend dev server (Vite) runs by default on port `5173` and will call the backend at `http://localhost:8000` for uploads.
